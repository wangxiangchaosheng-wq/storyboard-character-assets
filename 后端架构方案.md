# 知乎黑客松 · 赛道三 —— AI 历史模拟引擎 · 总纲索引

> **本文件是入口导航**：完整规范的开发文档已按模块拆分到 [`docs/`](docs/)，本文只保留决策记录与指引，避免多份文档内容漂移。拆分结构见 `docs/00`。

## 一句话

任何知乎话题（或自拟问题）→ 安全抓取/检索 → AI 多轮生成（数值公式 / 世界观剧本 / 角色设定）→ 校验对齐 → 玩家选角 → 通用推演引擎 → 桌面(Steam) / 网页 / 小程序 / 安卓 / iOS 都能玩。**北伐只是第一个剧本实例。**

## 核心决策（决策记录，论证见对应模块文档）

- 全 TypeScript（**不用 Python/FastAPI**）；引擎来自 D 盘 `scenario-engine` 迁移合并，数值全确定性。
- **防"越玩越慢"**：窗口化记忆 + RAG 归档 + 结构化摘要 + 角色并行 + 模型分级（docs/04）。
- **跨端**：桌面 Electron + React（与崇祯同形态、Steam 可发）；网页同套 SPA；安卓/iOS 用 Capacitor 壳；小程序用 Taro（React 语法）壳；共享层（contracts/engine-core/sdk）100% 复用（docs/09）。
- **安全红线**：凡服务端发起 URL 请求（用户链接抓取、cpk 生图等）→ **仅 http/https + host 校验 + 拒绝 localhost/环回/私有/保留地址**（`safeFetch`，docs/11）。
- **生图/多模态**：cpk-agnes（key 入 .env，base URL 待确认；未确认前 mock 模式开发，docs/06）。
- **通用管线**：链接 → Spec（input data hub）是枢轴，北伐只是首例 spec（docs/01）。

## 里程碑（详见 docs/10）

| 里程碑 | 内容 | Gate |
|---|---|---|
| M1 | 引擎 CLI 可跑完整局（5-8 回合） | `engine` 单测+存档哈希 |
| M2 | 话题→Spec 管线首用例通过 | 校验 Agent 拦截用例 |
| M3 | Fastify+SSE+SQLite；SPA 全流程 | 断线重连 |
| M4 | 桌面/网页/A 壳（安卓/iOS/小程序原型） | 一局双平台存档一致 |
| M5 | Steam 打包 + 演示 | 试玩视频+评审 |

## 4. 文档导航

| 模块文档 | 内容 | 所在包 |
|---|---|---|
| [docs/00-总览与开发规范](docs/00-总览与开发规范.md) | 导航 + 编码/Git/环境变量/规范 | — |
| [docs/01-话题管线](docs/01-话题管线.md) | 输入链接 → Spec（多轮生成+校验）| `topic-pipeline` |
| [docs/02-引擎核心](docs/02-引擎核心.md) | 回合机/metrics/rules/calc/存档 | `engine-core` |
| [docs/03-裁判系统](docs/03-裁判系统.md) | 双裁判与账本契约 | `engine-core/referee` |
| [docs/04-信息隔离与记忆](docs/04-信息隔离与记忆.md) | 隔离/防泄/记忆三层 | `engine-core/memory` |
| [docs/05-服务端与接口](docs/05-服务端与接口.md) | Fastify/REST/SSE/SQLite/错误码 | `serve` |
| [docs/06-LLM网关与多模态](docs/06-LLM网关与多模态.md) | provider 抽象/safeFetch/生图 | `llm` |
| [docs/07-历史数据与检索](docs/07-历史数据与检索.md) | Fact 模型/RAG/估算链 | `facts` |
| [docs/08-前端SPA](docs/08-前端SPA.md) | 7 页/组件树/SSE 客户端 | `apps/web` |
| [docs/09-跨端壳](docs/09-跨端壳.md) | Electron/Capacitor/Taro/Steam | `apps/*` |
| [docs/10-测试与验收](docs/10-测试与验收.md) | 测试金字塔/性能·安全专项/Gates | — |
| [docs/11-安全规范](docs/11-安全规范.md) | SSRF/密钥/注入/限流/审查清单 | 贯穿 |
| [docs/12-视觉与UI素材接口](docs/12-视觉与UI素材接口.md) | 素材清单/占位/美术交付流程（队友填充）+ 参考图布局 | `apps/web/assets` |

## 5. 环境与待办

- `.env.example` 模板待建：`AGNES_BASE_URL / AGNES_API_KEY / AGNES_CHAT_MODEL / AGNES_IMAGE_MODEL`、`SQLITE_PATH`、`LLM_CONCURRENCY` 等（见 docs/00 §6）。
- **等回答的消息**：cpk-agnes 的 base URL 与模型名 → 落 `AgnesProvider`（其余模块不阻塞）。
- 下一步执行建议：先在仓库 `packages/engine-core` 跑通 M1（迁移 scenario + 裁判件），同时建 `topic-pipeline` 首个 CLI 用例。