# 05 · 服务端与接口（serve）—— Fastify + REST + SSE + SQLite

> 产物：`packages/serve/`（Node ≥20 + Fastify + better-sqlite3）
> 字节面：全应用唯一后端入口；对(08) 前端/壳只暴露 `05` 契约；SSE 为主动推。

## 1. 职责范围

鉴权（客体）、路由、SSE 通道、SQLite 持久化、任务队列（话题管线/回合的异步执行）、上传静态（生图缓存）；**不含**领域逻辑（调用 02/01 实现）。

## 2. 技术规范

- 框架：Fastify 4（`@fastify/cors`、`@fastify/rate-limit`、`@fastify/static`、`@fastify/sensible`）。
- 运行：`node --experimental-strip-types`，或构建后 `dist/`（v4 起 tsx 亦可用，二选一固定）。
- DB：better-sqlite3（同步、WAL）；表见 §5；所有 SQL 参数化。
- SSE：POST 事件型 vs GET stream？——**GET /api/games/{id}/events**；事件格式见 §4。心跳 30s；断线用 `cursor`（事件 seq）续拉。
- 环境：`.env`（见 00 §6），非法 host 一律 500 系 安全错误。

## 3. 路由全集（v1）

| 方法 | 路径 | 说明 | 契约 |
|---|---|---|---|
| POST | /api/topics/ingest | 建任务 | 01 号 |
| POST | /api/topics/:id/build | 跑管线 | 01 号 |
| GET | /api/topics/:id/spec | 取 Spec | 01 号 |
| GET | /api/topics/:id/status | 进度 | 01 号 |
| POST | /api/topics/:id/abort | 停 | 01 号 |
| POST | /api/games | {specId, player} → 创建局 | 02 输入 |
| GET | /api/games/:id/state | 局级真值(服务端才能看) | 02 |
| GET | /api/games/:id/player-view | **玩家视角**（04 投影） | 04 |
| GET | /api/games/:id/map | 地图状态 | 02 |
| GET | /api/games/:id/options | 当前可选动作 | 02 |
| POST | /api/games/:id/decide | 提交玩家动作 | 回 Settlement |
| GET | /api/games/:id/storyboard | 故事板 JSON | 02 |
| GET | /api/games/:id/history | 事件日志(公开部分） | 02 |
| GET | /api/games/:id/ending | 结局 | 02 |
| GET | /api/games/:id/events | SSE 订阅 | 见 §4 |
| POST | /api/assets/image | 立绘生成缓存 | 06 号 |

> 所有 body 用 zod schema 校验（共享 `shared/contracts`）；未过 → 400 + 错误码。

## 4. SSE 事件板（单一清单，新增事件必须登记）

```
事件名: topics.ingest.progress / topics.build.*  (01)
       games.turn.report / games.state.update / games.map.update /
       games.character.available / games.ai.round.started / games.ai.round.complete /
       games.storyboard / games.error / heartbeat(30s) / games.ending
payload 均 {code, data?, ts}；error 时带 code（见 §6）
```
玩家侧 `fetch` → 前端 EventSource 或 fetch stream 同构封装（08）。

## 5. SQLite Schema（v1）

```sql
topics(id TEXT PK, url TEXT, title TEXT, status TEXT, spec_id, created_at)
specs(id TEXT PK, topic_id FK, payload TEXT/JSONB, version INT, warnings TEXT, created_at)
games(id TEXT PK, spec_id, player TEXT, seed INT, turn INT, state TEXT/JSONB,
      log TEXT/JSONB, created_at, updated_at, finished_at, hash TEXT)
turns(id INTEGER PK AUTOINC, game_id, turn_no, bundle TEXT, at)
assets(id TEXT PK, kind, url, model, created_at)   -- 生图缓存
kv(key TEXT PK, value TEXT)                        -- 幂等/光标
```
索引：games(spec_id)、turns(game_id, turn_no)、assets(kind)。

## 6. 错误码附录（全局唯一）

```
100x 输入校验：1001 body schema、1002 非法 URL(非 http/https)、1003 宿主风险(私网等)、1004 引用缺失 metric
200x 引擎：2001 存档损坏、2002 回合序号回退、2003 非法 action、2004 确定性冲突
300x 任务：3001 找不到任务、3002 已在运行、3003 超时
400x LLM：4001 provider 未配置、4002 超时、4003 限流、4004 内容被检(含泄漏 deny)
500x 存储：5001 SQLITE 写入失败
```
所有错误都返回 `{error: {code, message, details?}}` 且 ERROR 日志记录操作码。

## 7. 幂等与并发

- `decide` 幂等：每回合每玩家只能成功提交 1 次（DB `tokens` 记录 cursor）；重复 → 200 返回上次 result。
- 回合结算在单 DB 事务内完成（better-sqlite3 事务），防并发 weld。
- SSE 订阅断线 → 用 `Last-Event-ID` 从存档重放。

## 8. 验收清单

- [ ] 全路由 400/200/500 行为与文档一致；错误码表全覆盖
- [ ] SSE 心跳、重连续传（lastSeq）测试通过
- [ ] 100 局并发建局跑完无脏数据（SQLite WAL）
- [ ] 任何 route 在未认证(本地)可直接跑（MVP 无登录）；steam 壳走 `steamworks` 登录钩子