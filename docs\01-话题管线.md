# 01 · 话题管线（topic-pipeline）—— 任何话题 → ScenarioSpec

> 依赖：`llm`(06)、`facts`(07)、`serve`(05 只存进度)
> 产物：`packages/topic-pipeline/`
> 对应 v4 §8；这是"通用架构"的心脏。

## 1. 目标与边界

**目标**：输入一个知乎/网页链接或一段描述 → 产出**校验过**的 `ScenarioSpec`（可直接喂给 engine-core 开局）。
**不做**：不跑回合（那是 02）、不记住玩家（那是 05）、不保证史实全对（缺失部分给估算标注）。

## 2. 管线状态机（SSE 进度推进的是它）

```
idle ──ingest──► ingesting ──► analyzing ──► filling ──► generating ──► validating ──► revising(循环) ──► ready
   ▲                │ 失败/超时                       ▲ 校验不合格(≤3次) │
   └────────────────┴─────────── failed ────────────────────────────┘
```
阶段事件（见 05 号 SSE 表）：`ingest.progress / analyze.done / fill.done / gen.step / validate.report / spec.ready / spec.failed`。

## 3. 数据类型契约（真源：`shared/contracts/topic.ts`，此处为读者复制件）

```ts
// 输入
type TopicInput = { kind: 'url'; url: string } | { kind: 'text'; text: string };
type TopicBrief = {
  id: string; title: string;
  body: string; sections: Array<{ heading: string; text: string }>;
  asks: string[];            // 话题想回答/讨论的核心问题
  entities: EntityRef[];     // 人物/年代/地点
  sourceUrl?: string; takenAt: string;
};

// ② 数据填充
type Fact = {
  id: string;
  claim: string;                    // 一句话事实
  entity: string; 
  source: 'top_url' | 'search' | 'rag' | 'ai_estimate';
  estimated: boolean;               // 估算标记（关键！）
  confidence: number;               // 0..1（估算时给出）
  basis?: string;                   // 依据/推理
};
type FillResult = { facts: Fact[]; gaps: { topic: string; whyMissing: string }[] };

// ③ 生成结果（05 真源）
type MetricDef = { key: string; label: string; min: number; max: number; init: number };
type RuleDef = {
  id: string; key: string; effect: string;      // 形如 "agrand = base + troops*0.01"
  guard?: string; clamp: [number, number]; source: 'ai' | 'manual';
};
type CastDef = {
  id: string; name: string; camp: string;          // 玩家可选角色列表即由此出
  persona: { trait: string[]; goal: string; speakStyle: string; knowledgeBoundary: string };
};
type WorldDef = { title: string; premise: string; timeline: {quarter:number; events: string[]}[]; victory: string; lose: string };
type ScenarioSpec = { id: string; title: string; metrics: MetricSpec[];
  rules: RuleDef[]; world: WorldDef; cast: CastDef[]; initial: Record<string, number>;
  factsUsed: string[]; estimatedRate: number; creator: 'ai' | 'manual'; version: number };
// 全部字段 zod 校验过：min<max、init∈[min,max]、rules 无一引用未定义 metric、clamp 合法
```

## 4. 四司流水线（生成与校验的多轮协议）

实现按「四司」分工（`generate.ts` 编排，首行注释即契约）：

| 考据司 | `facts.ts: collectFacts` | 话题原文→事实/缺口；关键词直查预置史实库（`topicSearchTerms` 2-6 字短语 + 四位数年代）；缺口估算兜底 |
| 构演司 | `drama.ts: composeDrama` | 考据实体化 cast：isPersonName 语义词尾过滤概念实体（法/役/军…），立场对立保证（e≥1 与 c0 对立）、轮数/背景/事件来自史实 claim |
| 数值司 | `values.ts: deriveNumbers` | **AI 动态推导** metrics/rules：不为任何话题写死数值——LLM 依据考据事实动态定指标与公式；离线 mock 从事实抽「数字+单位」线索实化，无数字按语域给科目并标注估算；每条指标强制带 `provenance`（数值去哪找真实）、每条规则强制带 `derivations`（公式为什么这么推） |
| 审校司 | `validateSpec + checkAlignment` | 校验并回灌，至多 3 轮收敛；不收敛 → 标记 estimatedRate/warnings 提示玩家（绝不硬生成） |

- **数值溯源（玩家侧可见）**：spec 的 `provenance[]` 回答「这个数去哪查」（史料/检索词/换算口径/估算链），`derivations[]` 回答「这个公式怎么推」（依据与参照）；两链条与 metrics/rules 一一对应（单测保证），端上可展示「预估≈」之外的「为什么是这个数」。
- **mock/离线数值分支**：`MockChatProvider` 不可用 → `deriveNumbersOffline` 确定性推导（Facts 有「大军 12 万」→ 指标「大军(万)」start=12；无数字 → 军势/民调/存粮等语域科目并标注 estimate），全程可离线复演；语言参数与真实 LLM 路径同 schema。
- **mock/离线分支整体**：`MockChatProvider` 回显非结构化 JSON → 自动走考据直查 + 构演启发式，全程确定性、可离线验收；真实 LLM 时考据/构演/数值三司均走同 schema 输出，考据充分则内容更活。
- **检索接点**：`facts.search(q, {limit})` 是一个亲和门控的 BM25 检索（`MIN_SCORE`/精确命中/人物优先/语义亲和，见 07 §3）；serve 路由以 `limit:3` 透传，凑齐 ≥2 人物 cast 不成问题。
- 审校协议：`Generate(round) → Validate() → 若 fail → revision 回灌 → round+1 → Validate…`，上限 3 轮；第 3 轮仍未过 → 接受但标记 `estimatedRate` 与 warnings，ready 事件提示玩家「部分内容为 AI 估算」。

## 5. 公式安全子系统（先甲方级约束）

- 表达式语言：仅数字、`+ - * /`、`**`、`min/max/clamp/sqrt/abs`、`metric 引用`、每回合增量 `delta()`；**无变量赋值、无循环、无字符串**。
- 实现：`packages/engine-core/calc.ts` 的小型 tokenizer+parser（scenario-engine 已有同名模块，**直接迁移并可测**），解析结果缓存；越界 → clamp，解析失败 → spec 校验失败（validator 拦截）。
- 公式运行 3 次快照（-1/0/+1 回合）验证不发散，发散规则 remove + 降级为估算。

## 6. 缺数据时怎么办（核心卖点）

表触屏：topic → 检索（`facts` 07 号）；命中 < 阈值 → 对缺失 metric 请 LLM 出**估值 + 推理链**，填 `estimated:true`；裁判（03）读取 confidence 调权重（<0.5 视为"怎么回事"，扰动降低，结算朝中性浮动，不演大变局）。UI 用"预估≈"徽标提醒玩家这是 AI 估算。
> 规则：**估算绝不静默** — 必须 visible 到 final spec 的 `estimatedRate` 与每个 Fact 上。
> 数值也不静默：每个指标带 `provenance{source, how, estimate?}`（这个数去哪找真实值），每条规则带 `derivations{formula, why, ref?}`（这个公式为什么这么推）——由数值司动态产出并随 spec 出库（见 §4 数值司）。

## 7. 接口（先揍 REST，之后挂 SSE 进度）

```
POST /api/topics/ingest        { kind, url|text }  → 202 {topicId}   （异步任务）
POST /api/topics/{id}/build    {}                  → 202 启动管线（SSE 推进度/校验）
GET  /api/topics/{id}/spec     {}                  → 200 ScenarioSpec（ready 后）
GET  /api/topics/{id}/status   {}                  → {state, progress, round, lastRevision?}
POST /api/topics/{id}/abort    {}                  → 停止管线（可用于玩家放弃）
```
任务需幂等：同一 topicId 重复 build → 复用已生成 spec（cache），除非 `?force=true`。

## 8. 开发拆解与验收

| 步骤 | 里程碑 | 验收 |
|---|---|---|
| 1 | `TopicIngestor`：safeFetch+正文解析（标题/段落/实体） | 任意公开链接→TopicBrief；私网/非 http 拒绝 |
| 2 | `FillService`：facts 填充+估计 | 缺数据项有 `estimated` 标注+confidence |
| 3 | `SpecGenerator`（四司流水线） | `verify-topics.mts` 六话题全 ready：北伐=诸葛/司马懿、一战=兴登堡/斐迪南、二战=艾森豪威尔/希特勒、王安石、崇祯、创业——人物 cast 正确且无跨域实体混入；metrics 按话题动态生成（非统一常数），provenance/derivations 随 spec 出库 |
| 4 | `Validator`（最高 3 轮） | 可构造"明显无关生成"被拦截回炉的测试样例 |
| 5 | 公式引擎迁移+发散检测 | calc 全部通过既有单测；跑 3 快照检验 |

输出交予 engine-core（02）时必须通过 `ScenarioSpec` schema 校验，**builder 自身不破产逻辑**。