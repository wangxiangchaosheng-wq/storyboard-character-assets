# 02 · 引擎核心（engine-core）—— 回合机 / 数值 / 结算 / 存档

> 产物：`packages/engine-core/`（实质源自 D盘 `scenario-engine` 迁移 + v4 扩展）
> 依赖：03 裁判、04 记忆；被 05 serve 驱动；纯 TS 无框架、无 IO（IO 注入）。

## 1. 目标与硬约束

- **确定性**：同一 `GameInput`（含 seed、决策、外部裁判确定性分支）→ 逐字节相同的 `eventLog`。允许 LLM 分支 only 经 referee 打包成 `deterministicPack` 后进入状态。
- **零外呼**：引擎内禁止调 LLM/网络；所有不确定性由《裁判》(03) 以 `Settlement` 返回回填。
- **单可测试**：一回合的 4 大类 `@pipeline` 能 1ms 内跑完（不含 LLM）。
- 依赖规则：仅 `zod`；禁框架。

## 2. 从 scenario-engine 的迁移映射（复用表）

| scenario-engine（D盘） | engine-core（新） | 备注 |
|---|---|---|
| `src/core/types.ts` | `typings.ts` | 扩展 settlement/isolated 协议 |
| `src/core/metrics.ts` | `metrics.ts` | 原样 |
| `src/core/rules.ts` | `rules.ts` | 原样（RuleDef 由 AI 生成后同 schema）|
| `src/core/calc.ts` | `calc.ts` | 原样，迁移+单测 |
| `src/core/engine.ts` | `engine.ts` | 合并 referee、intel-gate hooks |
| `src/core/debate.ts` | `debate.ts` | 原样（廷议/对抗）|
| `src/core/agentLoop.ts` | `act.ts` | ReAct 循环（`maxRounds=30` 配置化）|
| `src/core/memory.ts` | `memory.ts` | 提升为 04 号文档的"三层记忆" |
| `src/core/llm.ts` | 交给 06 | 引擎只留 `AgentIO` interface |
| `src/domains/*` | `specs/` | 北伐等 spec 移至数据目录 |

## 3. 核心数据契约（真源 `shared/contracts/engine.ts`）

```ts
type Delta = { metric: string; by: number; why: string[] };
type GameAction = { playerId: string; kind: 'decision'|'diplomacy'|'promote'|'event'; choice: string; payload?: Record<string,unknown>; atTurn:number };
type Settlement = {           // 03 号文档同
  deltas: Delta[]; status: 'ok'|'partial'|'failed';
  reasons: { actor: string; text: string }[];
  longTerm: { key: string; value: unknown }[];   // 记忆固化
  mapChanges: { expr, value, label }[];          // 战略地图漂移
  publicNarrative: string;                       // 展示用（LLM 出/或模板）
  hidden: string;                                 // 仅裁判可见（防泄）
};
type TurnBundle = { turn: number; yearNo: number; quarterNo: number;
  decisions: { player: string; option: string }[];
  settlements: Settlement[]; eventTriggeredBy?: string[];
  nextQuestions: { to: string; question: string[] }[]; };

// 事件日志（单一事实源）
type EventLog = { turn: number; seq: number; kind: string; payload: unknown; ts: string }[];

// 存档
type SaveFile = { format: 'sim-v1'; specId: string; seed: number;
  turn: number; state: Record<string, number>; attitudes: Record<string, number[][]>;
  log: EventLog; mem: { withinWindow...; archiveIndex: string[] }; hash: string };
```

## 4. 回合时序（回合机核心契约）

```
sim.advance(input: { actions: GameAction[]; events: TurnEvent[]; rng: RNG }) → TurnResult
  1. 指令收敛：actions 表 → 角色 actor 各自 `act.ts`（可并行, 见 04 性能）
  2. 过滤/校验（info-gate 外阻止非法引用）
  3. 执行 rules（确定性 drift + reaction）─► delta 合并
  4. referee.adjudge(案例 pack)（03 号）→ official Settlement
  5. clamp/边界 -> state, attitudes 更新
  6. append eventLog（唯一 immutable 追加）
  7. produce TurnResult{ stateView, log, nextQuestions }
```
每回合**唯一状态快照** `GameState{ turn, metrics, attitudes, world, karma }`；全部旁路信息经 `info-gate`(04) 过滤后输出 `playerView`。

## 5. 确定性保障

- `calc` 无 `Math.random`；所有随机性来自 `rng(seed)` 的注入（`rng.shuffle/list`）。
- 回合 hash：`hash(eventLog)` 进存档 `hash`；重放即校验。
- 数值收敛检查：`clamp(min,max)` 前后做好；rules 增量全次数外挂式限制。

## 6. 演进与第三方显示

- embed 返回：`TurnView`（给 05/SPA 的最小序列化集）、`storyboard json`（故事板）。
- 地图：esan 地理（不渲染，只产漂移值），页面 canvas 自选。

## 7. 验收清单

- [ ] 北伐 spec 5-8 回合 CLI 可跑完（node `--experimental-strip-types` bin/cli.ts，沿 example）
- [ ] `pnpm check` + engine 单测（metrics/calc/rules/index→settlement）全绿
- [ ] 同一存档重放两遍 → eventLog 逐字节一致
- [ ] 回合哈希在结算后进行，防篡改
- [ ] engine 不 import 任何网络/fs（除 `fs` 存档读写由命令行封装在 bin/）