重大事件界面使用用户提供的 Group 30.svg（1672×941），结局图区域为1537×636。

已确认的重大裁判结果调用 major-story-event-assets；引擎普通结果先判定成立性和重大影响。单纯下令不触发。横图独立于右侧普通故事板，生成完成后显示，点击卷轴收起，保留原对话和选中角色。已阅事件在本机浏览器保存，刷新不重复弹出。

当前本机 SIM_ENGINE_URL 为空：尚未接入角色／裁判服务，实际对话自动产出结果仍依赖该服务。可通过现有已确认事件接口测试，不伪造胜负来演示。

生成使用已经授权的兼容 API；本次更新没有额外调用付费生图。
