# 04 · 信息隔离与记忆系统（Memory / Info-Gate / Anti-Leak）

> 位置：`packages/engine-core/memory/` + `packages/engine-core/intel/`（隔离门）
> 被：02(回合) 、03(裁判)、08(玩家视角) 使用。
> 参考：whodunit `VisibilityGate`/`InfoItem`/`LeakDetector`（`_refs/whodunit`）。

## 1. 双诉求

- **信息隔离**：每个角色只能看到"它应该知道"的世界；视角由 `playerView` 投影，绝不把未来/隐藏信息塞进 prompt。
- **记忆管理**：上下文窗口恒定（解决"越玩越慢"）——旧内容归档为摘要+RAG，只要非常之需才召回。

## 2. 信息模型

```ts
type InfoItem = {
  id: string; content: string;
  scope: 'public' | 'faction' | 'private';   // 可见域
  owners: string[];                           // 知悉者（角色/玩家）
  camps: string[];                            // 与阵营绑定，空则全可见
  createdTurn: number; revealTurn: number|null; // revealTurn 成了 = 自动公开
  seats: { metric?: string; map?: string };    // 关联的世界条目
};
```
`VisibilityGate`：
```
visible(item, viewer, state) :=
   item.scope === 'public' || item.owners.includes(viewer) ||
   (item.revealTurn !== null && turn >= item.revealTurn)
```
`playerView = {我的所见 metrics、我知的事件、当前问题、其他人可见之影响结果}` ；**整个世界状态从不在 prompt 里给任何 Agent**。

`LeakDetector`：在每次"裁判/Agent 输出前"扫描输出文本，`泄漏规则 = {若输出中出现 reward 未公开的 owner 信息或 revealTurn 未到的时间点 → 拦截}`；拦截 → 改判为"不知道"（默认 deny）并告警到日志。

## 3. 三层记忆（Memory Layering）

```
[0] current: 最近 1-2 回合事件（窗口，结构化）   ← Agent 推理即时可见
[1] archive: older 事件 → 回合摘要（JSON）入档
             摘要: {turn, key 一句话, deltas summary, 此人做过什么}
[2] perennial常青: 人物恒定事实/公理/世界常量（从不滚动）
回声：RAG 索引（07 号 facts）在 archive 上做检索召回，召回按 relevance 切割窗口
```
规则：
- 每回合结束，`archive(oldest)>WINDOW=2` → 生成 `structuralSummary`（纯确定性可先做：入档模板；叙事摘要交 LLM，但**摘要必须从结构化判定生成，防止摘要泄露**）。
- `archive > 30 回合` → 再做"年度大事记"压缩（管理员可调力度）。
- Agent prompt（06 号）三区拼装：`[常青][窗口原样][召回切片]`，总 token 预算由 05 统计与 06 护栏共同维护。

## 3b（编号保持）裁决时也用「裁判视图」：
裁判 input 用**全量**(objective)（允许 hidden），但其输出 `Settlement.hidden` 单独存，不由玩家可见——与 03 的 `playerView` 映射一致。

## 5. 记忆流水（给 engine 的接口）

```ts
type MemoryApi = {
  observe(turn, state, settlement): void;   // 每回合 append
  summarize(turn): SummaryJS;                // 切片->窗口缩急
  query(viewer, q, k=5): RememberedItem[];  // 窗口+RAG
  viewOf(viewer): PlayerMemory;              // 输出 prompt 块
};
```
`MemoryApi` 以上由 `engine.ts` 每回合在结算后调用；`viewOf` 是唯一能让 Agent prompt 读记忆的入口（防 Agent 私读）。实现要点：纯数据 + 可选 chroma（07 号 RAG 提供）；`summarize` 必须确定性可回放（seed 化）。

## 6. 验收与测试

- [ ] 泄密测试：构造角色 A 不知道事件 E，给 A 的 `viewOf` 断言不含 E
- [ ] LeakDetector：给 LLM 裁决注入含隐藏信息的文本 → 检出并 deny
- [ ] 窗口滑移：回合 5 时记忆不含回合 1 原文（archive 摘要存活、原文不存活）
- [ ] 同一 history 回放（同 seed）→ 每次 view 内容一致（确定性）
- [ ] token 预算：`viewOf` 长度恒定（≤3k），随回合增长而平