# 本机整合运行说明

双击仓库根目录的 `启动完整史境.command`。它依次启动队友后端（8787）、世界状态服务（4318）和原网页（3002）。浏览器打开 http://localhost:3002 。关闭服务时在启动终端按 Control+C。已有服务占用端口时不会重复启动。

首次安装：仓库根目录执行 `pnpm install --frozen-lockfile`；网页目录 `integrations/shijing-website` 执行 `npm ci`。需要 Node.js 22.6 或以上及 pnpm。

根目录 `.env` 用于队友后端，网页目录 `.env` 用于网页与世界状态服务。网页的 `SIM_ENGINE_URL` 应与根目录 `PORT` 对应，默认 http://127.0.0.1:8787 。凭证只保留在本地，不提交 GitHub；知乎 CLI 凭证存于系统钥匙串。

## 本次实际验证

- 统一启动三个服务成功。
- 配置的模型服务真实调用成功，接口基址需要包含 /v1。
- 官方知乎 CLI 检索成功。
- 知乎问题经 researchOnly 模式完成资料、人物整理并导入本地世界。
- 页面显示军队、城池与带来源说明的模拟数值。
- 独立测试议题行军一天：粮草 60000 → 54000；议题地图与总战略库一致。
- 世界状态/图库/桥接 42 项测试通过，后端 16 项测试通过，资料专用模式测试通过；后端类型检查通过。

## 接入边界

researchOnly 模式整理背景、人物和资料，不生成第二套结算公式。军队、补给与战斗继续由既有本地世界规则计算。知乎回答和模型输出不等同于经过核实的历史事实；数值保留模拟来源说明。

已接通队友的议题资料、人物设定与角色对话执行逻辑。未勾选行动时，角色通过队友后端根据本地可见局势回答；勾选行动时，仍由本地规则裁判校验并结算，不能由模型直接覆写兵力、粮草或战果。队友通用游戏裁判不是本地数值的第二个写入方。

在线生图已启用：缺少人物素材时依原 Skill 生成并验收；明确换题时创建新场景任务；本地已确认结算创建故事板候选，由重大事件 Skill 判断是否产出横向结局图。未通过验收的素材保留错误提示，不冒充已完成。

完整 monorepo 的 check 目前被 apps/desktop 缺少 node 类型依赖阻塞；这不影响已验证的网页和服务端启动，但不能将本次检查视为所有子项目全部通过。
