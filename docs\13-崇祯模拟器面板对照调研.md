# 崇祯模拟器面板对照调研与落地

> 调研日期：2026-09-07 · 调研对象：《历史模拟器：崇祯》（Steam appid 4304230）

## 一、游戏定位与本次调研链路

| 项 | 内容 |
|---|---|
| 正式名 | 《历史模拟器：崇祯》（俗称「崇祯模拟器」） |
| Steam appid | 4304230（本体；4629290 为 DLC 页面） |
| 开发商 | 青干工作室 |
| 类型 | 模拟 / 策略（AI 原生，LLM 世界推演，无预设 A/B 选项） |
| 发行 | 2026-05-07（国服本体获官方，内购「模型积分」制） |
| 调研链路 | 360 搜索 HTML（正式名/Steam 链接）→ store.steampowered.com/api/appdetails（官方元数据）→ 6 张商店截图 → Windows OCR 逐张解码 |

> 注：游戏官方名「历史模拟器：崇祯」在 Steam 商店搜索接口对简称「崇祯模拟器」没有命中，此前无法定位；词改为正式名后拿到 appid 4304230 的真实元数据与界面截图。

## 二、他方数据面板做法（截图 OCR 证据）

六张官方商店截图经 OCR 解码，其数据展示方式可归纳为四类：

1. **顶部资源条（常驻 HUD）**：主界面顶部一排数字带，如 `46.63万 · 2万 · 57% · 缺乏` ——直接显示绝对数值 / 占位 / 状态词；
2. **地图叠层视图切换**：主界面右下提供「标准 / 灾情 / 動亂 / 民心 / 城防 / 税收完成率 / 实际赋税」多套视图，同一张明朝地图按维度着色展示;
3. **「国策规划」面板**：分支树分配点数（"当前分支剩余分配点数：2"，管理/军事/學识/政治四维属性，均 0–10 级）；
4. **诏书/廷议**：自然语言输入诏书 + 大臣回应（本章非数值面板，仅记录）。

## 三、与本项目面板对照

| 维度 | 《历史模拟器：崇祯》 | 本项目（改前） | 本项目（改后） |
|---|---|---|---|
| 数值表达 | 资源条：绝对数值 + 总量/占位 | 大数字 + 排版无刻度 | 大数字 + 单位 + **区间占比 %** + 初始参考 |
| 趋势 | 资源条小幅变化（视图内可见） | 无变化提示 | **上轮 delta（▲▼）** |
| 健康状态 | 资源条直接标「缺乏/告急」 | 纯数字无状态 | **「充盈/见好/如常/見紧/告急」五档词** |
| 分类 | 视图切换（维度叠加） | 无 | **分域徽章（度支/仓货/军务/版籍/朝务）** |
| 底线预警 | 按资源「缺乏」即显 | 有；但**只告主指标** | **每个指标各自的 30% 底线规则** + 「危兆」行（fires 原文） |
| 信息隔离 | 数值揭示 = 展示 | 公式/区间保密 | 公式依旧剥离，仅公开展示层 |

### 已实际落地的改动（本轮）

1. **每指标独立底线规则**（engine-core）：修复原来「'th-crisis' 只绑主指标」的结构性缺陷；现由 values.ts 为每个指标生成一条 `th-crisis-<key>`（按该指标区间上限 30% 推算），8 条规则 8 档 acts：
   ```
   th-crisis-m1 · 「民户」跌破 540 时触发 · fires 「民户」跌破 540，事出异常，必生変局。
   th-crisis-m2 · 「兵額」跌破 36 时触发
   ```
2. **面板信息栏增强**（serve/web）：
   - 每张卡片加**当前在区间内占比 %**（如 `22%`）；
   - 当占比 ≤30%（与底线规则同阈值）时卡片进入 **warn** 态：红色左框 + 数字标红 + 「逼近底线」警告行；
   - 命中阈值 rule 的 fires 原文渲染为「危鬼」行（已有）。
3. **铺垫的诉求与规则、前端三者同源**：30% 线由指标定义推导，不是前端写死。

### 保留的两边差异（设计取向）

- 崇祯用地图叠加视图；本项目无地图（文本对话为主），用「卡片 + 轨道定位点」替代。
- 崇祯提供「国策树 + 四维角色属性」；本项目以「史料科目值」为数据主体，未引入角色属性双维（现有五档健康词可视为近似角色状态）。

## 四、验证

- `pnpm review`（7 步含类型、测试、游戏化词审计、单位露出、文语法、秘密扫描、真进程冒烟）→ **PASS 7/7**
- `serve` 启动自检：**7 / 7**
- 浏览器实测：新建话题「崇祯十七年：李自成率军破北京」（民户 700万 / 兵额 40万）→ 下三道勅令把民户追加到 390 万 → 面板绘制：
  - 民户卡：`390 万户 · ▼310 · 告急 · 22% · 初始 700` + **⚠逼近破格** + **⚠危鬼：「民户」跌破 540，事出异常，必生変局。**
  - 兵额卡：`120 万 · ▲80 · 充盈 · 100% · 初始 40`（无危鬼，未误报）
- 截图存证见会话产物（浏览器录制/截图条目）。

## 五、结论

对照 Steam《历史模拟器：崇祯》真实界面，本项目已在「数值+占位、趋势、状态词、底线每科独立、危鬼原文」五个维度追上并落地；剩余地图视图层因产品形态（无地图）不适用。此为目标「对比崇祯模拟器看别人如何做数据面板」的调研与整改闭环。