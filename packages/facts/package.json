{
  "name": "@sim/facts",
  "version": "0.1.0",
  "description": "历史数据与检索（docs/07）：SQLite 事实库 + BM25/余弦检索 + 估算器，降级链不中断",
  "private": true,
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --experimental-strip-types --test src/*.test.ts"
  },
  "dependencies": {
    "@sim/contracts": "workspace:*",
    "@sim/llm": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.5.0"
  }
}