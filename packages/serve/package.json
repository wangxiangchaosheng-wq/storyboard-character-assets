{
  "name": "@sim/serve",
  "version": "0.1.0",
  "description": "服务端：Fastify + REST + SSE + 管线/游戏路由（docs/05）",
  "private": true,
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "dev": "node --experimental-strip-types src/index.ts",
    "check": "tsc --noEmit",
    "test": "node --experimental-strip-types --test src/**/*.test.ts"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@sim/contracts": "workspace:*",
    "@sim/engine-core": "workspace:*",
    "@sim/facts": "workspace:*",
    "@sim/llm": "workspace:*",
    "@sim/topic-pipeline": "workspace:*",
    "fastify": "^4.28.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.5.0"
  }
}