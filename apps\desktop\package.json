{
  "name": "@sim/desktop",
  "version": "0.1.0",
  "description": "桌面壳（docs/09）：占位包 —— 后续接 Tauri/Electron 复用 web 构建产物",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --experimental-strip-types src/main.ts",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}