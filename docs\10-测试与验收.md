# 10 · 测试与验收（整体策略 + 里程碑）

> 贯穿每个模块；本文是"怎么验收产品级"，非单测抄写。
> 原则：**引擎与安全是硬门槛**，UI/壳是可打磨项。

## 1. 测试金字塔（三层）

```
E2E（每里程碑 1 次）：一局完整 5-8 回合（CLI 或 SPA）→ 可生成结局
契约测试（交叉模块）：types/schema → 编译 + zod sample；SSE 事件顺序
单元（具体模块）：见各模块验收清单（引擎/裁判/记忆/safeFetch 是红线）
```

## 2. 强制红色线（不过不能合 PR）

| 项 | 用例示例 | 涉包 |
|---|---|---|
| 引擎确定性 | 同 seed 重跑两遍 → eventLog 逐字节一致 | engine |
| 隔离不泄露 | 角色 A 的 viewOf 不含未可知事件 | engine/memory |
| 泄密拦截 | LeakDetector 命中含 hidden 文本 → deny | memory |
| safe URL | 127.0.0.1 / 10.0.0.8 / 169.254.x / internal / localhost 全拒 | llm/安全 |
| 数值稳定 | rules clamp、发散检测（3 快照） | engine |
| spec 有效性 | 任意 build 输出须过 zod（schema >0 错误） | 01 |

## 3. 性能专项（回应"越玩越慢"）

- 工具：`scripts/perf.mjs` 用引擎跑 N 轮，输出每回合 token 数 + 耗时。
- **目标线：单角色单回合 prompt ≤3k token，回合耗时 ≤10s（不含用户等待）。**
- 编译断言：50 回合游戏，第 40 回合 ≤ 第 5 回合的 1.5× 平均耗时（窗口化+RAG 生效）。
- 回归：每 PR 触发 `perf:smoke`（只跑 10 均衡）。

## 4. 安全专项验收

- `safeFetch` 拒绝表单测（本地环回/私网/保留/非 http）。
- 密钥扫描：CI `gitleaks`/grep `AGNES_API_KEY=xxx` 禁止；.env 不进 git。
- 接口：完整 `decide` 非法 payload → 400；未授权私有信息不可达（playerView 检查）。
- 依赖：`pnpm audit`（发布零高危）。

## 5. 各模块验收清单索引（已各写进模块文档）

01→§8 02→§7 03→§7 04→§6 05→§8 06→§8 07→§7 08→§9 09→§7 11→§3

## 6. 里程碑验收 Gate（M1…M5）

- **M1（引擎）**：P0 关：CLI 北伐全片跑完 ≈ <30s 轮
- **M2（管线）**：以"崇祯/王安石"链接各产一 Spec，validator 通过
- **M3（服务）**：SPA 全流程（含 SSE 断线重连演示）
- **M4（多端）**：桌面(本地模式) + web 同局存档；小程序 MVP prototype
- **M5（发布）**：Steam 安装包装机、试玩 video、评审演示视频

每个 Gate 由验收清单生成一个 `docs/checklist-Mx.md` 表单，`✅/⚠️/❌` 列 + 评审签。

## 7. 缺陷与回归

- 详情集中 `issues/` 目录（每 bug 一 md：复现、期望、现象、影响、fix PR）。
- 合入主干一律配 regression test（若红线）。